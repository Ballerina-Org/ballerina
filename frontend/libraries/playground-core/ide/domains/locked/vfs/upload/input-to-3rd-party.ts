// import {INode, Meta, FlatNode, NodeId} from "./model";
// import { Map } from "immutable";
//
//
// // TODO: FileList -> VFS (upload),  and VFS -> 3RD (for selecting only once time, then readonly)  and 3RD to VFS (simple selector selector)
// const segments = (p: string) => p.split("/").filter(Boolean);
// const idOf = (segments: string[]) => segments.join("/");
//
