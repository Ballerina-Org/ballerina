export type IdeFormProps = {
    spec:any, 
    specName: string, 
    //entityName: string, 
    typeName: string,
    setState: (state: any) => void,
    launcher: string,
    path: string [],
    // launcherConfigName: string,
}